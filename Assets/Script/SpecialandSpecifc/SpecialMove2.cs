using UnityEngine;

public class SpecialMove2 : PlayerMove
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    protected virtual void PlayerStart2()
    {
        
    }

    protected virtual void Awake()
    {
       
    }
    
}
